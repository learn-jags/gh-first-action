name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greetings
        run: |
          echo "Hello first github action"
          ls -la
      - name: Print bye
        run: echo "Finished"
      
